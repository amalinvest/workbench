# Default Redis URL pre-filled in the onboarding form during development.
# The desktop sidecar reads REDIS_URL when launched; this var is only used
# to seed the connect form in dev mode.
VITE_DEFAULT_REDIS_URL=redis://localhost:6379
